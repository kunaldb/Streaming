# Batch generation (local or any Python)
pandas>=1.3.0

# Optional: for streaming on Databricks, use the cluster's PySpark / Spark;
# no need to install pyspark locally unless you run streaming locally.
